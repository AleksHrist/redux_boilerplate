export const Types = {
    //You can call the reducers through actions if you want ...

    // GLOBAL_HOSPITAL_INFO_UPDATE: "global/updateHospitalInfo"
}

//And export them like so

/*export const setHospitalData = (data: any) => ({
    type: Types.GLOBAL_HOSPITAL_INFO_UPDATE,
    payload: data
})*/
